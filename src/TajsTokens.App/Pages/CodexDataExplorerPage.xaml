<Page
    x:Class="TajsTokens.App.Pages.CodexDataExplorerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Spacing="3">
            <TextBlock Text="Codex Data Explorer" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="Developer fallback for source health, auxiliary Codex stores and raw read-only SQLite inspection. This surface preserves source shape and does not create product semantics."
                       Opacity="0.72" TextWrapping="Wrap" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <Button x:Name="SourcesButton" Content="Source workbench" Click="OnSourcesClicked" />
            <Button x:Name="RawButton" Content="Raw SQLite" Click="OnRawClicked" />
            <TextBlock x:Name="FilterHintText" VerticalAlignment="Center" Opacity="0.68" TextWrapping="Wrap" />
        </StackPanel>
        <Frame x:Name="ExplorerFrame" Grid.Row="2" />
    </Grid>
</Page>
